# Handoff: 2026-09-17 morning

Verification and four small features. `CLAUDE.md` has the standing rules; this
note covers what changed, what was verified, and what is left.

## What changed

- **CMB-9 verified at rush hour.** Congestion score 0.06 (clear-hour baseline)
  moved to 0.33 at 07:47 EDT; drive 41 → 71 min. Closed.
- **Fork moved (config only).** The owner's `route.decision_point` sat about
  1 km past the Exit 9 ramp, so the drive-to-lot leg ran to the next exit and
  back: 12 min for 6.7 km against Google's 6 min for 4.4 km. Four live Routes
  calls proved it. Moved upstream of the ramp in `config.toml` and pushed as
  secret version 4. Lesson: check fork placement with a fork→lot call whose
  distance is near the straight line, not by eye.
- **CMB-29** transit request departs at now + drive-to-lot + buffer, rail only.
- **CMB-30** `?from=origin` / `--from origin` measures the whole trip from home.
- **CMB-31** optional `[route.parking]` with a walk to the door.
- **CMB-32** arrival clock on the verdict and in the spoken line.
- **CMB-33** every `[route.*]` place is `lat`, `lon`, `label`, `addl_walk_mins`;
  the two old walk keys are rejected by name. Main 056a701, revision
  `switchtender-00008-b4d`, config secret v6. 234 tests.

All in main 933758d, Cloud Run revision `switchtender-00006-phl`, verified live
(200 / 400 / 401 and both spoken lines). 230 tests.

## What is left

- **CMB-31 active** since 10:05 EDT: destination is the work door, garage in `[route.parking]`, walk 5 min (owner estimate, to be timed). Secret v5, revision `switchtender-00007-tk6`.
- **Rail leg vs Google Maps.** Still 65 vs 59 after CMB-29. Watch the sheet;
  candidates are the final walk and a different transfer.
- **Sheet rows from 2026-09-17 before 09:38 EDT** used the old fork. Cut there
  if charting drive-to-lot.
- `?from=origin` stays a curl or browser call; owner said the phone macro does not need it.
