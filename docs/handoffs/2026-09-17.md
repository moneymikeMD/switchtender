# Handoff: 2026-09-17 morning

One session, 07:45 to 10:20 EDT. It verified the congestion signal at rush
hour, found and fixed a misplaced fork in the owner's config, and shipped five
tickets that make the two options end at the same door, time the train
correctly, speak the arrival, and give a whole-trip estimate from home.
`CLAUDE.md` has the standing rules. Read this once, then work from the
tickets.

## What changed this session

- **Congestion signal proven live at rush hour.** CMB-9 closed on a 07:47
  run: score 0.33 against the 0.06 clear-hour baseline, drive 71 vs 41 min.
- **The fork was in the wrong place.** The owner's `route.decision_point` sat
  about 1 km past the Exit 9 ramp, so the drive to the lot ran to the next
  exit and back (12 min, 6.7 km) where Google Maps said 6 min, 4.4 km. Moved
  upstream of the ramp in `config.toml` (gitignored) and pushed as config
  secret v4. Config only; no code.
- **Train timed off the lot arrival, rail only (CMB-29).** The transit
  request carries `departureTime` = now + drive to lot + platform walk and
  `allowedTravelModes = [RAIL, SUBWAY]`.
- **Whole-trip estimate from home (CMB-30).** `GET /verdict?from=origin` and
  `npm start -- --from origin`. Spoken line opens "Starting from home."
  Unknown `from` is a 400, checked after the key.
- **Both options end at the same door (CMB-31).** Optional `[route.parking]`;
  driving is routed there and its walk added. Active in the owner's config:
  destination is the workplace door, the garage is the parking place, walk 5.
- **Arrival spoken (CMB-32).** "You would arrive at 10:58 AM." for the chosen
  option; both arrivals on the verdict JSON, ISO and 12-hour clock.
- **One shape for every place (CMB-33).** Every `[route.*]` table is
  `lat`, `lon`, `label`, `addl_walk_mins`. The two old walk keys and any
  unknown key fail at load, naming the key.

Commits on main: 933758d (CMB-29 to 32), 056a701 (CMB-33), plus handoffs.
Cloud Run revision `switchtender-00008-b4d`, config secret v6.

## Verified vs assumed

Verified, by running it:

- `npm test`: 234 pass, no network.
- Local `npm start` from the fork and from the origin after every change.
- Deployed service after the last deploy: `/health` 200, `?from=garage` 400,
  no key 401, `/verdict` and `/verdict?from=origin` both return the new spoken
  line with an arrival clock. Sheet write printed "logged to sheet" on every
  local run.
- Four live Routes calls (fork vs owner's inflection point, each to the lot)
  were the evidence for moving the fork.

Assumed, not verified:

- The 5-minute walk from the garage to the door. Owner's estimate.
- That the phone macro still speaks the longer line cleanly. Nothing in the
  macro changed and the JSON shape only gained fields, but no phone run
  happened this session.

## What remains

- **CMB-34** rail leg 65 min vs Google Maps 59 from the same station. Filed
  at wrap-up; the plan is in the ticket. Not urgent.
- Sheet rows from 2026-09-17 before 09:38 EDT used the old fork, and rows
  before 10:05 EDT ended the drive at the garage with no walk. Cut there if
  charting drive-to-lot or drive-through.

## Decisions only a human can make

- Time the garage-to-door walk and set `route.parking.addl_walk_mins` if 5 is
  wrong. Then `scripts/deploy-cloud-run.sh --update-config`.
- Owner said the phone macro does not need `from=origin`; it stays a curl or
  browser call.

## Suggested next

Nothing is startable without the owner: CMB-34 wants same-minute pairs the
owner collects from the Maps app. Open the next session by reading this note
and recalling `switchtender` and `cmb` from memory-graph. Tool shells need
`. /opt/homebrew/opt/nvm/nvm.sh && nvm use` before `npm`, and the five keys
come from 1Password as in the session-start command in
`docs/handoffs/2026-09-16-evening.md`.
